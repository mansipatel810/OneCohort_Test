# ── Application ──────────────────────────────────────────
base.url=http://localhost:4200
username=Admin
password=admin123
# OneCohort uses role-based login (User ID + Role), no password field
super.admin.user.id=SA001
valid.service.line.id=SL-001
valid.poc.id=USR-40002
valid.cohort.id=COH-10001

# ── Browser ──────────────────────────────────────────────
# Options: chrome | firefox | edge
browser=chrome
headless=false

# ── Timeouts (seconds) ───────────────────────────────────
implicit.wait=10
explicit.wait=15
page.load.timeout=30
